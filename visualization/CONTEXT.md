# General context
Here you'll create a dashboard using all the data into the gold layer. Use evidence.dev so the dashboard doesnt need my pc to be on (run it into github); create all the needed files here.
- Ask for my github user and token when needed so it can be set there.
- The dashboard needs to be concised, give useful insights. Use different graphs as needed.
- The graphs inside the dashboard need to adapt to the data it will use (best for insights communication). Each graph axis present the proper title.
- Make the dashboard totally interactive, with filters, interactive graphs, interactive tables (for example, if in a bar chart I select one class with a click, the entire dashboard needs to be filter based on that selection, this includes all graphs and tables).
- All the elements (graphs, tables, etc) have to be interactive with each other elements.
- Create graphs that interact with multiple variables inside the data (games, genres, dates, ratings, platforms, etc, be creative dont cross the line of simplest).
- If a bar chart is added but have too many classes, just show the top 10.
- for name axis, be sure that the classes are readable.
- For data cards, if the use of a avg is intended, evaluate if average or meadian is statistically correct, select the proper one (Dont add any comment of the related choice).
- Analyze the possibility of "error: Timeout while initializing database", plan and execute based on that
- Evaluate if its possible to use the gold layer data directly from azurite to evidence.dev (no parquet files into the visualizations folder), let me know the choice and ask for confirmation.


At the end add the created files into the claude.md file directory structure diagram (ONLY MAIN FILES, do not add subfiles).